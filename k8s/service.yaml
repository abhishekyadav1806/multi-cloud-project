apiVersion: v1
kind: Service
metadata:
  name: multi-cloud-project-service
spec:
  type: NodePort
  selector:
    app: multi-cloud-project
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080